# Credentials Packer — ne jamais committer
packer/local.pkrvars.hcl

# Artefacts Packer — ne jamais committer
packer/output/
packer/.packer_cache/

# Clé SSH par défaut générée par Packer — ne jamais committer
packer/default_id_ed25519

# Terraform
.terraform/
terraform.tfstate
terraform.tfstate.backup
.terraform.lock.hcl
.terraform.tfstate.lock.info
# Outils & éditeurs
.claude/
.vscode/
/.idea/
*.iml

# Fichiers système
.DS_Store
debug.txt
crash.log

# Helm
Chart.lock

# Credentials
auth_file.json
